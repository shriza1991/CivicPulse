services:
  - type: web
    name: nivaran-backend
    env: python
    region: singapore
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: nivaran-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: nivaran-redis
          property: connectionString
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: FRONTEND_ORIGIN
        value: https://nivaran.vercel.app
      - key: GEMINI_API_KEY
        sync: false
      - key: SENDGRID_API_KEY
        sync: false

databases:
  - name: nivaran-db
    databaseName: nivaran
    user: nivaran_user
    plan: free

  - type: redis
    name: nivaran-redis
    plan: free
    ipAllowList: []

